[project]
name = "subjective-egoneticsai"
version = "0.1.0"
description = "Self-cybernetics autonomous agent execution engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
